
#include <stdio.h>

int main() {
    int n, i;
    printf("Enter the value of n: ");
    scanf("%d", &n);

    int ara[n];
    printf("Enter element of an array: ");
    for(i = 0; i < n; i++){
        scanf("%d", &ara[i]);
    }


return 0;
}
