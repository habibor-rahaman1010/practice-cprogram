//power in c program...

#include <stdio.h>
#include <math.h>

int main() {
    int x = 5;
    int y = 5;

    int c = pow(x, y);

    printf("Power is: %d \n", c);

    float s;
    s = sqrt(48);
    printf("Square root is: %f \n", s);

return 0;
}
