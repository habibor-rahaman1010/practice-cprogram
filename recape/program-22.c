// find out which is big between two number...

#include <stdio.h>

int main(){
    int a = 20, b = 8;

    if(a > b){
        printf("%d is gater then %d", a, b);
    }
    else{
        printf("%d is gater then %d", b, a);
    }

return 0;
}
